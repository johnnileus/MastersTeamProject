#pragma once
#include <string>

namespace Enums
{
    enum class AnimationType {
        Player_Idle,    
        Player_Walk,
        Player_Jump
    };

    enum class RenderObjectType
    {
        Default,
        Skinned
    };

    enum class WeaponType
    {
        Pistol,
        Rifle,
        Shotgun
    };
    
}

